docker run -d \
-e METHOD=aes-256-gcm \
-e "ARGS=--plugin v2ray-plugin --plugin-opts server;tls;host=证书域名;path=/v2ray;cert=/root/.acme.sh/证书域名/fullchain.cer;key=/root/.acme.sh/证书域名/证书域名.key -u" \
-e PASSWORD=YourPassWord \
-v /root/.acme.sh:/root/.acme.sh \
--user root \
--name=shadowsocks-libev \
-p 8388:8388/tcp \
-p 8388:8388/udp \
--restart=always \
acrisliu/shadowsocks-libev
